<!DOCTYPE html>
<html lang="en">
  <%- include('includes/head', { title: 'Symptom Analysis', nonce: nonce }) %>
  <link rel="stylesheet" href="styles.folder/possibleDisabilities.css" />

  <body>
    <div class="page-wrapper">
      <%- include('includes/header', { nonce: nonce }) %>

      <div class="container">
        <h1>Symptoms Analysis</h1>
        <input type="hidden" id="csrfToken" value="<%= csrfToken %>" />

        <form id="symptom-form">
          <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
          <h2>New Symptoms</h2>
          <div id="symptomEntriesContainer">
            <div class="symptom-entry">
              <div class="input-group">
                <label>
                  System Affected
                  <div class="custom-dropdown">
                    <% systems.forEach(function(system) { %>
                    <div class="dropdown-item" data-system="<%= system %>">
                      <%= system %>
                      <div class="hover-image">
                        <img src="/images/<%= systemImages[system] %>" alt="<%= system %>" />
                      </div>
                    </div>
                    <% }); %>
                  </div>
                </label>
                <label>
                  Symptoms:
                  <input
                    type="text"
                    class="symptoms"
                    placeholder="Enter at least 5 symptoms (comma separated)"
                    required
                  />
                </label>
              </div>
              <button type="button" class="remove-entry-button">Remove Entry</button>
            </div>
          </div>
          <div class="button-row">
            <button type="button" id="addEntryButton">Add Another Entry</button>
          </div>
          <div class="analyze-section">
            <button id="analyzeButton">Analyze Symptoms</button>
          </div>

          <input type="text" name="honeypot" style="display: none" />
        </form>

        <!-- Results Display -->
        <div id="results"></div>
      </div>

      <!-- Styles -->
    <style>
  .custom-dropdown {
    border: 1px solid #ccc;
    width: 300px;
    max-height: 300px;
    overflow-y: auto;
    position: relative;
    background: white;
  }
  .dropdown-item {
    padding: 10px;
    cursor: pointer;
    position: relative;
    border-bottom: 1px solid #eee;
  }
  .dropdown-item:hover {
    background-color: #f5f5f5;
  }
  .hover-image {
    display: none;
    position: absolute;
    left: 100%;
    top: 0;
    width: 200px;
    border: 1px solid #ccc;
    background: white;
    z-index: 10;
  }
  .dropdown-item:hover .hover-image {
    display: block;
  }
  .hover-image img {
    width: 100%;
    height: auto;
  }
</style>

<div class="custom-dropdown">
  <% systems.forEach(function(system) { %>
  <div class="dropdown-item">
    <%= system %>
    <div class="hover-image">
      <img src="/images/<%= systemImages[system] %>" alt="<%= system %>" />
    </div>
  </div>
  <% }); %>
</div>

      

      <!-- Scripts -->
      <script src="/js/mobile-drawer.js" nonce="<%= nonce %>"></script>
      <script src="/js/symptomAnalysis.js" nonce="<%= nonce %>" defer></script>

      <script nonce="<%= nonce %>">
        document.addEventListener('DOMContentLoaded', function () {
          console.log('Symptom Analysis page loaded');

          // You can remove the systemImages object here if not used for other things.
          // And you can remove the JS event listener for .body-part change if not needed.
        });
      </script>

      <div class="spacer"></div>
      <%- include('includes/footer') %>
    </div>
  </body>
</html>

